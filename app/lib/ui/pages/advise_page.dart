import 'package:flutter/material.dart';

class AdvisePage extends StatelessWidget {
  const AdvisePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('💬 Advise Page', style: TextStyle(fontSize: 24)),
    );
  }
}
