import 'package:flutter/material.dart';

class MyPlantPageLogin extends StatelessWidget {
  const MyPlantPageLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🌿 My Plant Page', style: TextStyle(fontSize: 24)),
    );
  }
}